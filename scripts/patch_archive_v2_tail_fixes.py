from pathlib import Path

path = Path('/Users/neoclaw/repos/ifa-data-platform/src/ifa_data_platform/archive_v2/runner.py')
text = path.read_text()

if "SOURCE_FIRST_60M_FAMILIES = {" not in text:
    text = text.replace(
        "SOURCE_FIRST_DAILY_FAMILIES = {\n    'index_daily',\n    'macro_daily',\n    'announcements_daily',\n    'news_daily',\n    'research_reports_daily',\n    'investor_qa_daily',\n    'dragon_tiger_daily',\n    'limit_up_detail_daily',\n    'limit_up_down_status_daily',\n    'sector_performance_daily',\n}\n\n",
        "SOURCE_FIRST_DAILY_FAMILIES = {\n    'index_daily',\n    'macro_daily',\n    'announcements_daily',\n    'news_daily',\n    'research_reports_daily',\n    'investor_qa_daily',\n    'dragon_tiger_daily',\n    'limit_up_detail_daily',\n    'limit_up_down_status_daily',\n    'sector_performance_daily',\n}\nSOURCE_FIRST_60M_FAMILIES = {\n    'equity_60m',\n    'etf_60m',\n    'index_60m',\n    'futures_60m',\n    'commodity_60m',\n    'precious_metal_60m',\n}\nZERO_OK_DAILY_FAMILIES = {\n    'investor_qa_daily',\n    'research_reports_daily',\n}\n\n"
    )

old = "        kind = meta['kind']\n        if family_name in SOURCE_FIRST_DAILY_FAMILIES:\n            rows = self._fetch_source_first_daily_rows(family_name, business_date)\n            return self._write_daily_rows(meta['dest_table'], business_date, rows, note=meta.get('note', 'source-first daily archived'))\n"
new = "        kind = meta['kind']\n        if family_name in SOURCE_FIRST_DAILY_FAMILIES:\n            rows = self._fetch_source_first_daily_rows(family_name, business_date)\n            if not rows and family_name in ZERO_OK_DAILY_FAMILIES:\n                return 0, [meta['dest_table']], 'completed', 'source-empty but truthful zero-row day', None\n            return self._write_daily_rows(meta['dest_table'], business_date, rows, note=meta.get('note', 'source-first daily archived'))\n        if family_name in SOURCE_FIRST_60M_FAMILIES:\n            rows = self._fetch_source_first_60m_rows(family_name, business_date)\n            return self._write_intraday_rows(meta, business_date, rows, note=meta.get('note', 'source-first 60m archived'))\n"
if old in text:
    text = text.replace(old, new, 1)

# patch index_daily direct fetch to use universe, not trade_date only
old = """    def _fetch_index_daily_direct(self, business_date: str) -> list[dict]:\n        client = self._get_tushare_client()\n        rows = client.query('index_daily', {'trade_date': business_date.replace('-', '')})\n        return [self._normalize_record(dict(r)) for r in rows]\n"""
new = """    def _fetch_index_daily_direct(self, business_date: str) -> list[dict]:\n        client = self._get_tushare_client()\n        out = []\n        seen: set[tuple[str, str]] = set()\n        with engine.begin() as conn:\n            universe = conn.execute(text(\"\"\"\n                select distinct ts_code\n                from ifa2.index_basic_history\n                where ts_code is not null\n                order by ts_code\n            \"\"\")).scalars().all()\n        for ts_code in universe:\n            try:\n                rows = client.query('index_daily', {'ts_code': ts_code, 'trade_date': business_date.replace('-', '')})\n            except Exception:\n                rows = []\n            for row in rows:\n                rec = self._normalize_record(dict(row))\n                key = (str(rec.get('ts_code') or ts_code), str(rec.get('trade_date') or business_date))\n                if key in seen:\n                    continue\n                seen.add(key)\n                out.append(rec)\n        return out\n"""
if old in text:
    text = text.replace(old, new, 1)

anchor = "    def _fetch_history_rows(self, table: str, date_col: str, business_date: str) -> list[dict]:\n"
if "def _fetch_source_first_60m_rows(self, family_name: str, business_date: str) -> list[dict]:" not in text:
    helper = """    def _fetch_source_first_60m_rows(self, family_name: str, business_date: str) -> list[dict]:\n        dispatch = {\n            'equity_60m': lambda d: self._fetch_stk_mins_direct(self._load_symbol_universe('stock_basic_history'), d, '60min'),\n            'etf_60m': lambda d: self._fetch_stk_mins_direct(self._load_symbol_universe('fund_basic_etf_history'), d, '60min'),\n            'index_60m': lambda d: self._fetch_idx_mins_direct(self._load_symbol_universe('index_basic_history'), d, '60min'),\n            'futures_60m': lambda d: self._fetch_ft_mins_direct(self._load_symbol_universe('futures_history'), d, '60min'),\n            'commodity_60m': lambda d: self._fetch_ft_mins_direct(self._load_symbol_universe('commodity_history'), d, '60min'),\n            'precious_metal_60m': lambda d: self._fetch_ft_mins_direct(self._load_symbol_universe('precious_metal_history'), d, '60min'),\n        }\n        return dispatch[family_name](business_date)\n\n    def _load_symbol_universe(self, table: str) -> list[str]:\n        with engine.begin() as conn:\n            return [x for x in conn.execute(text(f\"select distinct ts_code from ifa2.{table} where ts_code is not null order by ts_code\")).scalars().all() if x]\n\n    def _fetch_stk_mins_direct(self, ts_codes: list[str], business_date: str, freq: str) -> list[dict]:\n        client = self._get_tushare_client()\n        out = []\n        for ts_code in ts_codes:\n            try:\n                rows = client.query('stk_mins', {'ts_code': ts_code, 'freq': freq, 'start_date': business_date + ' 09:00:00', 'end_date': business_date + ' 16:00:00'})\n            except Exception:\n                rows = []\n            for row in rows:\n                rec = self._normalize_record(dict(row))\n                rec['bar_time'] = rec.get('trade_time') or rec.get('trade_date')\n                rec['instrument_code'] = rec.get('ts_code') or ts_code\n                out.append(rec)\n        return out\n\n    def _fetch_idx_mins_direct(self, ts_codes: list[str], business_date: str, freq: str) -> list[dict]:\n        client = self._get_tushare_client()\n        out = []\n        for ts_code in ts_codes:\n            try:\n                rows = client.query('idx_mins', {'ts_code': ts_code, 'freq': freq, 'start_date': business_date + ' 09:00:00', 'end_date': business_date + ' 16:00:00'})\n            except Exception:\n                rows = []\n            for row in rows:\n                rec = self._normalize_record(dict(row))\n                rec['bar_time'] = rec.get('trade_time') or rec.get('trade_date')\n                rec['instrument_code'] = rec.get('ts_code') or ts_code\n                out.append(rec)\n        return out\n\n    def _fetch_ft_mins_direct(self, ts_codes: list[str], business_date: str, freq: str) -> list[dict]:\n        client = self._get_tushare_client()\n        out = []\n        for ts_code in ts_codes:\n            try:\n                rows = client.query('ft_mins', {'ts_code': ts_code, 'freq': freq, 'start_date': business_date + ' 09:00:00', 'end_date': business_date + ' 16:00:00'})\n            except Exception:\n                rows = []\n            for row in rows:\n                rec = self._normalize_record(dict(row))\n                rec['bar_time'] = rec.get('trade_time') or rec.get('trade_date')\n                rec['instrument_code'] = rec.get('ts_code') or ts_code\n                out.append(rec)\n        return out\n\n"""
    text = text.replace(anchor, helper + anchor)

path.write_text(text)
print('patched tail fixes into archive_v2 runner')
